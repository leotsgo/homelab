apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: immich-scheduled-backup
  namespace: databases
spec:
  immediate: true
  schedule: "0 0 4 * * *"
  backupOwnerReference: cluster
  cluster:
    name: immich
