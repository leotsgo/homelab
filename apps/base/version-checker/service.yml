apiVersion: v1
kind: Service
metadata:
  name: version-checker
  labels:
    app: version-checker
spec:
  selector:
    app: version-checker
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
