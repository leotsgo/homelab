apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# version-checker runs in the monitoring ns (next to kube-state-metrics + alloy,
# which scrapes it and remote_writes to the zeus Prometheus).
namespace: monitoring

resources:
  - rbac.yml
  - deployment.yml
  - service.yml
