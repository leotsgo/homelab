apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: wallabag-cluster
  namespace: wallabag
spec:

  instances: 1
  storage:
    size: 1Gi

  resources:
    requests:
      memory: 300Mi

  enableSuperuserAccess: true

  bootstrap:
    recovery:
      source: wallabag-cluster
      database: wallabag
      owner: wallabag

  backup:
    barmanObjectStore:
      destinationPath: s3://leotsgo-data/wallabag-db
      endpointURL: https://s3.us-east-005.backblazeb2.com
      s3Credentials:
        accessKeyId:
          name: b2
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: b2
          key: ACCESS_SECRET_KEY
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "14d"

  externalClusters:
  - name: wallabag-cluster
    barmanObjectStore:
      destinationPath: s3://leotsgo-data/wallabag-db
      endpointURL: https://s3.us-east-005.backblazeb2.com
      serverName: wallabag-cluster
      s3Credentials:
        accessKeyId:
          name: b2
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: b2
          key: ACCESS_SECRET_KEY

  managed:
    services:
      disabledDefaultServices:
        - ro
        - r
