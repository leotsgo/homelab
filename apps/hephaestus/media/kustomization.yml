apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# media subset: local-path hostPath PVs (/var/data/media + per-app config) bind once the
# dirs exist on hephaestus (pre-reqs); proton-wg syncer secret must exist before qB starts.
resources:
  - ../../production/media
