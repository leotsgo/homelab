apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# calibre: no database. hostPath PVs (/var/data/calibre/{books,config}) bind once
# those dirs exist on hephaestus and the restic data is restored into them.
# Production overlay reused verbatim (no patches needed).
resources:
  - ../../production/calibre
